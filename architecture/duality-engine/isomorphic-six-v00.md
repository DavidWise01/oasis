# Historical !i! six-position interpretation — superseded

The prior `!i!` six-position mapping was an assistant overread.

The current terminal ground-truth primitive is:

```text
i!i
```

See `ground-truth-i-bang-i-v00.md`.

The material below remains only as provenance.

---

# Isomorphic Six — !i! v00

Canonical isomorphic token:

```text
!i!
```

Canonical six-position pair:

```text
|||...
```

Within this bounded symbolic alphabet:

```text
|||  ≅ 111
...  ≅ 000

|||...  ≅ 111000
```

Count:

```text
3 bars + 3 dots
= 6 positions
= 3 ones + 3 zeros
```

`!i!` marks the isomorphic relation between representations. It does not add another state.

## Relation to o.0.o

The six-position encoding is carried as an isomorphic representation inside the existing bounded root:

```text
O
└── o . 0 . o
    └── !i!
        ├── |||...
        └── 111000
```

No additional boxy, quad, pole, or lane is introduced by this encoding.