# o.0.o — Isomorphic Boxy Root v00

Everything here remains symbolic inside the bounded `O` engine.

## Canonical root

```text
o . 0 . o
```

Equivalent scalable notation:

```text
o^n . 0^n . o^n
```

Permutation form:

```text
[ boxy1 [ permutate o ] ]
          .
[ boxy2 [ permutate 0 ] ]
          .
[ boxy3 [ permutate o ] ]
```

## Dot rule

The `.` token is not an empty separator and is not a fourth box.

```text
boxy[.] = ground-truth blast zone
bounded to `0`
```

So the structural read is:

```text
boxy1(o)
   ↓
GT blast zone `.` bounded to 0
   ↓
boxy2(0)
   ↓
GT blast zone `.` bounded to 0
   ↓
boxy3(o)
```

## Isomorph

The two outer `o` boxies are the same symbolic family on opposite sides of the center:

```text
o_left  .  0_center  .  o_right
   \                     /
    \_____ isomorphic __/
```

The center remains `0`. The dots witness the transitions through bounded ground truth.

## Probability-engine relation

The earlier 3-body / 4-quad probability notation is not a separate top-level engine. It is one expanded/permuted definition carried inside this `o.0.o` root.

No one-to-one assignment between E-lanes and boxies is added here unless explicitly specified.

## Bound

```text
O contains o.0.o
o.0.o returns inside O
```

`O` supplies the closed Ouroboros bound; `o.0.o` supplies the three-box isomorphic root with two ground-truth blast zones.