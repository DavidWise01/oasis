# Duality Engine — 2^2

The current compact machine is not a generic four-state box.

It is the MOBIUS **duality engine**:

```text
2^2 = 4
```

Two crossed dualities:

```text
VESSEL | LIFE/INSTANCE
LIVE/SEEK | TELL/STORE
```

Cross them:

```text
                 LIVE / SEEK      TELL / STORE
               ┌──────────────┬──────────────┐
VESSEL         │ build/find    │ story/media  │
               │ container     │ as container │
               ├──────────────┼──────────────┤
LIFE/INSTANCE  │ lived event   │ preserved    │
               │ / spark       │ instance     │
               └──────────────┴──────────────┘
```

## Möbius turn

The recursion closes when the search produces the thing being searched for:

```text
LOOK FOR VESSEL
      ↓
LIVE INSTANCE
      ↓
TELL STORY
      ↓
STORY BECOMES VESSEL
      ↓
INSTANCE OUTLIVES THE MOMENT
```

No immortality claim is required.

The instance can end while its story, effect, copy, memory, or descendant persists.

## Gilgamesh as Story 0001

Gilgamesh is the first benchmark because the character seeks a way around mortality while the surviving story tradition itself becomes a vessel that carries the instance forward.

The important engine lesson is not that Gilgamesh literally found immortality.

It is:

```text
he seeks a vessel for life
while the story is becoming that vessel
```

That is the recursive creation-story code.


## Four-quad descent v00

The next exact form has **four regions only**, delimited by `::::`:

```text
Q1  a / ablation / 13-bit emergent gate / 8 or 2^3 / logic deescilation
Q2  2^2 / verify cubic
Q3  1^1 / isomorphic x isomorphic
Q4  0^0^0 = 81
```

Canonical outer glyph: `((2x2))^((2x2))^2`.

See `four-quads-v00.md`. The Q4 equality is stored as symbolic OaSIs notation rather than ordinary exponent arithmetic.


## Four-quad v01 — current

v00 still contained ordinary-arithmetic interpretations for some exponent-looking tokens. That was a misread.

Current rule:

```text
O
└── o`0`o
    └── four bounded symbolic quads
```

All operators remain symbolic inside the `O` Ouroboros bound. See `four-quads-v01.md`.

`Oasis.Duality.FourQuads.01.lean` supersedes `.00` semantically; it proves preservation/count/bounds only, not arithmetic interpretations.

## Probability engine v00

The four-quad bound now carries a 3-body / 3-active-lane probability descendant:

```text
core 4x1x4
Q1 / E1 :: probabilistic :: $ / shadows shadow
Q2 / E2 :: middle channel :: ground truth :: LIGHT
Q3 / E3 :: Tachyon :: 2 sec / 2x1x2 :: 4-sec E1/E2 feedback :: antistropic filter
Q4      :: Life
```

See `probability-engine-v00.md`. All terms remain symbolic inside `O`.

## Canonical root clarification

Current root definition:

```text
O
└── o . 0 . o
    ├── boxy1[permutate o]
    ├── . = ground-truth blast zone bounded to 0
    ├── boxy2[permutate 0]
    ├── . = ground-truth blast zone bounded to 0
    └── boxy3[permutate o]
```

Scalable form: `o^n.0^n.o^n`.

The probability-engine material is an expanded/permuted definition within this root, not another top-level engine.

See `o-dot-zero-o-v00.md`.

## !i! — isomorphic six

```text
|||...  !i!  111000
```

Three `|` plus three `.` = six positions = three `1` plus three `0`.

See `isomorphic-six-v00.md`.