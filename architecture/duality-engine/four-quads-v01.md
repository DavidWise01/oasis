# Duality Engine — Four Quads v01

Everything in this layer is **symbolic within a bound**.

Canonical bound:

```text
O
```

Canonical isomorphic root:

```text
o`0`o
```

Read structurally:

```text
left o  ↔  `0`  ↔  right o
   \                 /
    \_____  O  _____/
```

The two `o` forms are isomorphic views/sides around Root0. `O` is the closed Ouroboros return/bound.

No exponent-looking token in this layer is required to mean ordinary exponent arithmetic.

## Four quads

```text
((2x2))^((2x2))^2

[[ {
  4 quads ::::

  Q1 {
    a
    ablation
    must pass 13-bit emergent prims
    8 or 2^3
    logic deescilation
  }

  ::::

  Q2 {
    2^2
    verify cubic
  }

  ::::

  Q3 {
    1^1
    isomorphic x isomorphic
  }

  ::::

  Q4 {
    0^0^0 = 81
  }

} ]]
```

The `::::` separators delimit exactly four bounded symbolic regions.

## Rule

```text
inside O:
  preserve glyph
  preserve order
  preserve bound
  preserve isomorph

do not:
  force symbolic operators into conventional arithmetic
  invent a fifth quad
  replace o`0`o with another ontology
```

## Canonical descent

```text
O
└── o`0`o
    └── ((2x2))^((2x2))^2
        ├── Q1 :: a / ablation / 13-bit / 8 or 2^3 / logic deescilation
        ├── Q2 :: 2^2 / verify cubic
        ├── Q3 :: 1^1 / isomorphic x isomorphic
        └── Q4 :: 0^0^0 = 81
             ↺
             O
```

That return is the Ouroboros closure.