# Compact Engine — 2^2

Current machine:

```text
2^2 = 4
```

Two binary dimensions produce four structural positions.

```text
      B0   B1
    ┌────┬────┐
A0  │ 00 │ 01 │
    ├────┼────┤
A1  │ 10 │ 11 │
    └────┴────┘
```

The axes are deliberately unnamed. No additional semantics are inferred until they are explicitly supplied.

Rule:

```text
given  : 2^2
derive : 4 positions
do not : invent labels
```

This is the newer compact form; older larger loop material remains historical context.
