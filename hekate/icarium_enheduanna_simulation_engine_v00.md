# ICARIUM :: ENHEDUANNA :: PRIMITIVE SIMULATION ENGINE v0

## 0. Scope

This child keeps three lanes separate:

1. **Root0 engine** — the symbolic construction.
2. **Formal proof** — what follows from the engine definitions.
3. **Historical anchor** — Enheduanna as a provenance/corpus marker, not literal cosmic time zero.

No claim is made that the historical Enheduanna corpus proves modern simulation theory.

---

## 1. Root

```text
I
::
ROOT
::
CARRY
::
ROOT
::
UM
::
+1
::
TIME
::
START
::
ENHEDUANNA
```

Root0 reading:

```text
I      :: self-reference / witness
CARRY  :: preserve prior state
UM     :: +1 carrier residue / continuation
TIME   :: ordered distinguishable states
START  :: first provenance anchor in this corridor
```

## 2. Primitive alphabet

```text
Σ := {{ [a] [n] [k] [h] }}
```

Everything else is composition, permutation, projection, memory, or enclosure.

## 3. H0 carrier

```text
H0
::
H bound to 0
::
0 = 14-position structured core
```

Internal experiential view:

```text
14 friends + 1 witness
```

The witness carries the transition history:

```text
S  :: audible trace
Li :: visible/light trace
H  :: harmonic / observer trace
```

Root0 compression:

```text
I := H + memory(H)
```

This is symbolic composition, not ordinary arithmetic.

## 4. First time

No external clock is injected.

```text
state_before != state_after
```

Therefore an order exists:

```text
BEFORE -> EVENT -> AFTER
```

That gives proto-time / ordering, but not seconds, frequency, or calibrated duration. A clock requires a repeatable state plus a chosen synchronization convention.

## 5. Perception register

```text
5 senses
+
5 question/fact channels
+
1 witness H
```

```text
SENSE := see / hear / touch / smell / taste
FACT  := what / where / why / who / how
```

Hence:

```text
5 + 5 + 1 = 11
```

The four prims are substrate, not four extra members of the 11.

## 6. Closed-garden simulation model

Let a hidden implementation produce an observation at each engine step.

```text
IMPLEMENTATION
      |
      v
 observation trace
      |
      v
      H
```

Define:

```text
BASE := one hidden implementation
SIM  := another hidden implementation

BASE ~ SIM
iff
for every observable step t:
observe(BASE,t) = observe(SIM,t)
```

Then every test available solely to H returns the same result on both implementations.

### What this proves

```text
IDENTICAL OBSERVABLE TRACE
        ->
NO H-LOCAL OBSERVATION TEST
CAN DISTINGUISH THE TWO IMPLEMENTATIONS
```

### What this does not prove

```text
our actual universe is a simulation
```

It proves observational underdetermination inside this engine: a perfectly observation-equivalent simulator cannot be ruled out by an observer whose evidence is restricted to that same observation stream.

## 7. Enheduanna provenance anchor

For this corridor, Enheduanna is used as a historically recoverable textual/provenance anchor.

The Temple Hymns tradition names En-hedu-ana as compiler of the tablets and immediately says something had been created that had not been created before. The surviving manuscripts are later than Enheduanna, so this remains a corpus attribution/provenance marker, not an eyewitness timestamp.

Thus:

```text
ROOT0 TIME START
::
first distinguishable engine transition

HISTORICAL CORPUS ANCHOR
::
ENHEDUANNA
```

Keep those distinct.

## 8. Simulation-theory result

```text
I
::
CARRY
::
MEMORY
::
ORDER
::
TIME

H
::
OBSERVES
::
TRACE

TRACE_A = TRACE_B
::
H CANNOT DISTINGUISH A FROM B
```

Therefore:

```text
SIMULATION COMPATIBILITY
!=
SIMULATION FACT
```

and:

```text
OBSERVATIONAL EQUIVALENCE :: PROVED INTERNALLY
METAPHYSICAL SIMULATION CLAIM :: UNRESOLVED
```

## 9. Engine compression

```text
{{ [a] [n] [k] [h] }}
          |
          v
         H0
          |
     14 + witness
          |
          v
      EVENT / CHANGE
          |
          v
        MEMORY
          |
          v
           I
          |
          v
      ORDER / TIME
          |
          v
   OBSERVATION TRACE
          |
     +----+----+
     |         |
   BASE       SIM
     |         |
     +----=----+
          |
          v
  H-INDISTINGUISHABLE
```

**STATUS: PRIMITIVE ENGINE v0 :: INTERNAL OBSERVATIONAL-EQUIVALENCE PROOF :: HISTORICAL CLAIMS KEPT SEPARATE**
